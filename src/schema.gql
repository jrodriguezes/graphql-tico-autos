# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type VehicleType {
  _id: ID!
  ownerId: Int!
  brand: String!
  model: String!
  year: Float!
  price: Float!
  mileage: Int!
  status: String!
  observations: String
  plateId: String!
  imageUrl: String
}

type VehiclePageType {
  data: [VehicleType!]!
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}

type Query {
  vehicles: [VehicleType!]!
  vehicle(id: String!): VehicleType
  filteredVehicles(filters: VehicleFiltersInput): VehiclePageType!
  myVehicles: [VehicleType!]!
}

input VehicleFiltersInput {
  brand: String
  model: String
  minYear: Int
  maxYear: Int
  minPrice: Float
  maxPrice: Float
  status: String
  page: Int = 1
  limit: Int = 8
}